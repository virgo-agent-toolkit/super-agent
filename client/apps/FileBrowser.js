define('apps/FileBrowser', function () {
  'use strict';

  FileBrowser.title = 'FileBrowser';
  return FileBrowser;
  function* FileBrowser(call, ...folders) {
    return function (win) {
    };
  };

});
